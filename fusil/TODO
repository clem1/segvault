Fusil TODO list
===============

 * Factorize code responsible to rename the session on process exit
   (share code between Debugger and CreateProcess)
 * Protect the terminal using setsid(), or setpgrp(), or setpgid(), or ...
 * Implement initgroups() for CreateProcess?
 * finish destruction of WatchProcess

